# Phase 2 baseline retrieval. torch arrives transitively via sentence-transformers
# (~2 GB); the bge-small model (~130 MB) downloads on first use. CPU is fine for
# the current corpus size.
sentence-transformers>=3.0
pgvector>=0.3
numpy>=1.26
psycopg[binary]>=3.2
python-dotenv>=1.0
pytest>=8.0

# Phase 4 serving: re-ranker runs via ONNX Runtime (no torch needed at serve time)
onnxruntime>=1.18

# Phase 6 API + answer layer
fastapi>=0.115
uvicorn[standard]>=0.30
psycopg-pool>=3.2
anthropic>=0.40
httpx>=0.27  # FastAPI TestClient
