requests>=2.32
python-dotenv>=1.0
pytest>=8.0
beautifulsoup4>=4.12
psycopg[binary]>=3.2
pypdf>=4.0
# Optional OCR fallback for scanned PDFs (also needs the Tesseract binary on PATH):
# pytesseract>=0.3
# pypdfium2>=4
