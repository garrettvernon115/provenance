# Phase 3: training-data generation (LLM positives + BM25 hard negatives).
# Cross-encoder training + ONNX export deps arrive in Phase 4.
anthropic>=0.40
psycopg[binary]>=3.2
python-dotenv>=1.0
pytest>=8.0
# transitively needed: build_training_data reuses backend/retrieval.py, whose
# embedding module imports numpy (no torch is loaded for lexical mining)
numpy>=1.26

# --- Phase 4: cross-encoder training + ONNX export -------------------------
# torch (CPU) arrives transitively via sentence-transformers. Training runs on
# CPU here; the same requirements reproduce on a free Colab/Kaggle GPU at scale.
sentence-transformers>=5.0
datasets>=2.19
accelerate>=0.30
onnx>=1.16
onnxruntime>=1.18
onnxscript>=0.5  # required by torch>=2.x torch.onnx.export
